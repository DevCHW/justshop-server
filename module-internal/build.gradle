project(':module-internal:core-web') {
    bootJar.enabled = false

    dependencies {
        implementation project(':module-core')
    }
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}