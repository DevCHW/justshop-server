spring:
  output:
    ansi:
      enabled: always
  profiles:
    active: dev

  application:
    name: order-service

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:~/justShopApplication
    username: sa
    password:

  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
